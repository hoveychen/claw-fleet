<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- ── App Sandbox intentionally OFF ──
         Fleet spawns `claude` children whose Bash tool must freely EXEC the
         user's dev toolchains (node/pnpm/cargo/go/python in /opt, /usr/local,
         ~/.cargo, nvm/fnm/volta, …) and read+write anywhere in the user's file
         system. Under the sandbox this required an ever-growing
         temporary-exception path whitelist that still EPERM'd every new
         toolchain location. Disabling the sandbox removes that whole class of
         failures. Trade-off: the app can no longer ship on the Mac App Store
         (sandbox is mandatory there) — Fleet is distributed as a signed
         Developer-ID app instead. -->

    <!-- ── Hardened Runtime: JIT / unsigned memory ──
         The Tauri WKWebView needs these; they are Hardened-Runtime entitlements,
         NOT sandbox ones, so they stay regardless of sandbox state. -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>
