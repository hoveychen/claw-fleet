name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── Pre-build: Linux x86_64 fleet-cli probe (bundled into every app package) ─
  # Built once, shared across all platform builds so each bundle can SCP the
  # correct Linux binary to remote servers without needing internet on the remote.
  linux-x64-probe:
    name: Build Linux x86_64 probe binary
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set version from tag
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          sed -i "s/^version = \".*\"/version = \"${VERSION}\"/" src-tauri/Cargo.toml

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libglib2.0-dev \
            libgtk-3-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            xdg-utils

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Build fleet-cli for Linux x86_64
        run: |
          cargo build --release --bin fleet-cli \
            --manifest-path src-tauri/Cargo.toml \
            --target x86_64-unknown-linux-gnu
          cp src-tauri/target/x86_64-unknown-linux-gnu/release/fleet-cli /tmp/fleet-linux-x64

      - name: Upload probe binary
        uses: actions/upload-artifact@v4
        with:
          name: fleet-linux-x64-probe
          path: /tmp/fleet-linux-x64

  linux-arm64-probe:
    name: Build Linux ARM64 probe binary
    runs-on: ubuntu-22.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set version from tag
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          sed -i "s/^version = \".*\"/version = \"${VERSION}\"/" src-tauri/Cargo.toml

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libglib2.0-dev \
            libgtk-3-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            xdg-utils

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-linux-gnu

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Build fleet-cli for Linux ARM64
        run: |
          cargo build --release --bin fleet-cli \
            --manifest-path src-tauri/Cargo.toml \
            --target aarch64-unknown-linux-gnu
          cp src-tauri/target/aarch64-unknown-linux-gnu/release/fleet-cli /tmp/fleet-linux-arm64

      - name: Upload probe binary
        uses: actions/upload-artifact@v4
        with:
          name: fleet-linux-arm64-probe
          path: /tmp/fleet-linux-arm64

  # ── Build all platforms (macOS: sign only, notarization handled separately) ─
  build:
    needs: [linux-x64-probe, linux-arm64-probe]
    name: Build — ${{ matrix.name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS (Apple Silicon)
            runner: macos-latest
            rust_target: aarch64-apple-darwin
            args: --target aarch64-apple-darwin
            fleet_bin: fleet-cli
            fixed_suffix: macos-arm64

          - name: macOS (Intel)
            runner: macos-latest
            rust_target: x86_64-apple-darwin
            args: --target x86_64-apple-darwin
            fleet_bin: fleet-cli
            fixed_suffix: macos-x64

          - name: Windows (x64)
            runner: windows-latest
            rust_target: x86_64-pc-windows-msvc
            args: --target x86_64-pc-windows-msvc
            fleet_bin: fleet-cli.exe
            fixed_suffix: windows-x64

          - name: Windows (ARM64)
            runner: windows-latest
            rust_target: aarch64-pc-windows-msvc
            args: --target aarch64-pc-windows-msvc
            fleet_bin: fleet-cli.exe
            fixed_suffix: windows-arm64

          - name: Linux (x86_64)
            runner: ubuntu-22.04
            rust_target: x86_64-unknown-linux-gnu
            args: --target x86_64-unknown-linux-gnu
            fleet_bin: fleet-cli
            fixed_suffix: linux-x64

          - name: Linux (ARM64)
            runner: ubuntu-22.04-arm
            rust_target: aarch64-unknown-linux-gnu
            args: --target aarch64-unknown-linux-gnu
            fleet_bin: fleet-cli
            fixed_suffix: linux-arm64

    runs-on: ${{ matrix.runner }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set version from tag
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          sed -i.bak "s/^version = \".*\"/version = \"${VERSION}\"/" src-tauri/Cargo.toml
          rm -f src-tauri/Cargo.toml.bak

      - name: Download Linux x64 probe
        uses: actions/download-artifact@v4
        with:
          name: fleet-linux-x64-probe
          path: src-tauri/resources

      - name: Download Linux arm64 probe
        uses: actions/download-artifact@v4
        with:
          name: fleet-linux-arm64-probe
          path: src-tauri/resources

      - name: Mark probe binaries executable
        shell: bash
        run: chmod +x src-tauri/resources/fleet-linux-x64 src-tauri/resources/fleet-linux-arm64

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libglib2.0-dev \
            libgtk-3-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            xdg-utils

      # ── Pre-build fleet CLI sidecar ─────────────────────────────────────────
      # Must run before tauri-action so the real binary exists in binaries/ and
      # overwrites the build.rs placeholder. tauri-action then signs + bundles it.
      - name: Build fleet CLI sidecar
        shell: bash
        run: |
          # Build the CLI binary (named fleet-cli to avoid collision with tauri externalBin "fleet")
          cargo build --release --bin fleet-cli \
            --manifest-path src-tauri/Cargo.toml \
            --target ${{ matrix.rust_target }}
          mkdir -p src-tauri/binaries

          EXT=""
          if [ "$RUNNER_OS" = "Windows" ]; then EXT=".exe"; fi

          # Copy as fleet-{triple} — tauri externalBin strips the triple and bundles it as "fleet"
          cp "src-tauri/target/${{ matrix.rust_target }}/release/${{ matrix.fleet_bin }}" \
             "src-tauri/binaries/fleet-${{ matrix.rust_target }}${EXT}"

          # Linux: deb.files needs a generic "fleet-linux" name (same binary, generic path)
          if [ "$RUNNER_OS" = "Linux" ]; then
            cp "src-tauri/target/${{ matrix.rust_target }}/release/fleet-cli" \
               "src-tauri/binaries/fleet-linux"
            chmod +x "src-tauri/binaries/fleet-linux"
          fi

          if [ "$RUNNER_OS" != "Windows" ]; then
            chmod +x "src-tauri/binaries/fleet-${{ matrix.rust_target }}"
          fi

      # Sign on macOS — APPLE_ID/PASSWORD/TEAM_ID intentionally omitted
      # so Tauri skips notarization (handled by notarize.yml)
      - name: Import macOS signing certificate
        if: runner.os == 'macOS'
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          MACOS_CERTIFICATE_PWD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
          MACOS_KEYCHAIN_PASSWORD: ${{ secrets.MACOS_KEYCHAIN_PASSWORD }}
        run: |
          if [ -z "$MACOS_CERTIFICATE" ]; then
            echo "MACOS_CERTIFICATE not set — skipping"
            exit 0
          fi
          echo "$MACOS_CERTIFICATE" | base64 --decode > certificate.p12
          security create-keychain -p "$MACOS_KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$MACOS_KEYCHAIN_PASSWORD" build.keychain
          security import certificate.p12 -k build.keychain \
            -P "$MACOS_CERTIFICATE_PWD" -T /usr/bin/codesign
          security set-key-partition-list \
            -S apple-tool:,apple: -s -k "$MACOS_KEYCHAIN_PASSWORD" build.keychain
          rm certificate.p12

      - name: Install frontend dependencies
        run: npm ci

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ''
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
        with:
          args: ${{ matrix.args }}
          includeUpdaterJson: false

      # ── Collect artifacts ───────────────────────────────────────────────────

      - name: Collect artifacts (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          mkdir -p dist-artifacts
          # App bundle (DMG) — rename to fixed filename
          find src-tauri/target/${{ matrix.rust_target }}/release/bundle/dmg \
            -name "*.dmg" | head -1 | xargs -I{} cp {} dist-artifacts/claude-fleet-${{ matrix.fixed_suffix }}.dmg
          # Standalone fleet binary (for users who want it without the app)
          cp src-tauri/target/${{ matrix.rust_target }}/release/fleet-cli \
             dist-artifacts/fleet-${{ matrix.fixed_suffix }}

      - name: Collect artifacts (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          mkdir -p dist-artifacts
          # App installer (NSIS) — rename to fixed filename
          find src-tauri/target/${{ matrix.rust_target }}/release/bundle/nsis \
            -name "*.exe" | head -1 | xargs -I{} cp {} dist-artifacts/claude-fleet-${{ matrix.fixed_suffix }}-setup.exe
          # Standalone fleet binary
          cp src-tauri/target/${{ matrix.rust_target }}/release/fleet-cli.exe \
             dist-artifacts/fleet-${{ matrix.fixed_suffix }}.exe

      - name: Collect artifacts (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          mkdir -p dist-artifacts
          # .deb — rename to fixed filename
          find src-tauri/target/${{ matrix.rust_target }}/release/bundle/deb \
            -name "*.deb" | head -1 | xargs -I{} cp {} dist-artifacts/claude-fleet-${{ matrix.fixed_suffix }}.deb 2>/dev/null || true
          # AppImage — rename to fixed filename
          find src-tauri/target/${{ matrix.rust_target }}/release/bundle/appimage \
            -name "*.AppImage" | head -1 | xargs -I{} cp {} dist-artifacts/claude-fleet-${{ matrix.fixed_suffix }}.AppImage 2>/dev/null || true
          # Standalone fleet binary
          cp src-tauri/target/${{ matrix.rust_target }}/release/fleet-cli \
             dist-artifacts/fleet-${{ matrix.fixed_suffix }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.rust_target }}
          path: dist-artifacts/
          if-no-files-found: error

  # ── Publish release with all artifacts ─────────────────────────────────────
  publish:
    name: Publish Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: release-*
          path: all-artifacts
          merge-multiple: true

      - name: List artifacts
        run: ls -lh all-artifacts/

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Generate release notes
        run: |
          cat >> release_notes.md << 'EOF'
          ## Downloads

          ### App Installers

          | | Platform | Architecture | File |
          |---|---|---|---|
          | <img src="https://raw.githubusercontent.com/hoveychen/claude-fleet/main/docs/icon-apple.svg" width="24"> | macOS | Apple Silicon (M1/M2/M3/M4) | `claude-fleet-macos-arm64.dmg` |
          | <img src="https://raw.githubusercontent.com/hoveychen/claude-fleet/main/docs/icon-apple.svg" width="24"> | macOS | Intel | `claude-fleet-macos-x64.dmg` |
          | <img src="https://raw.githubusercontent.com/hoveychen/claude-fleet/main/docs/icon-windows.svg" width="24"> | Windows | x64 | `claude-fleet-windows-x64-setup.exe` |
          | <img src="https://raw.githubusercontent.com/hoveychen/claude-fleet/main/docs/icon-windows.svg" width="24"> | Windows | ARM64 | `claude-fleet-windows-arm64-setup.exe` |
          | <img src="https://raw.githubusercontent.com/hoveychen/claude-fleet/main/docs/icon-linux.svg" width="24"> | Linux | x86_64 | `claude-fleet-linux-x64.deb` / `claude-fleet-linux-x64.AppImage` |
          | <img src="https://raw.githubusercontent.com/hoveychen/claude-fleet/main/docs/icon-linux.svg" width="24"> | Linux | ARM64 | `claude-fleet-linux-arm64.deb` / `claude-fleet-linux-arm64.AppImage` |

          ### `fleet` CLI (standalone binaries)

          | | Platform | Architecture | File |
          |---|---|---|---|
          | <img src="https://raw.githubusercontent.com/hoveychen/claude-fleet/main/docs/icon-apple.svg" width="24"> | macOS | Apple Silicon | `fleet-macos-arm64` |
          | <img src="https://raw.githubusercontent.com/hoveychen/claude-fleet/main/docs/icon-apple.svg" width="24"> | macOS | Intel | `fleet-macos-x64` |
          | <img src="https://raw.githubusercontent.com/hoveychen/claude-fleet/main/docs/icon-windows.svg" width="24"> | Windows | x64 | `fleet-windows-x64.exe` |
          | <img src="https://raw.githubusercontent.com/hoveychen/claude-fleet/main/docs/icon-windows.svg" width="24"> | Windows | ARM64 | `fleet-windows-arm64.exe` |
          | <img src="https://raw.githubusercontent.com/hoveychen/claude-fleet/main/docs/icon-linux.svg" width="24"> | Linux | x86_64 | `fleet-linux-x64` |
          | <img src="https://raw.githubusercontent.com/hoveychen/claude-fleet/main/docs/icon-linux.svg" width="24"> | Linux | ARM64 | `fleet-linux-arm64` |

          ## Installing the `fleet` CLI

          The `fleet` CLI is bundled inside the app installer and also available as a standalone binary.

          **macOS** — after installing the app:
          ```sh
          sudo ln -sf "/Applications/Claude Fleet.app/Contents/MacOS/fleet" /usr/local/bin/fleet
          ```

          **Linux** — the `.deb` package installs `fleet` to `/usr/bin/fleet` automatically.
          For AppImage users, download the standalone binary and place it in your `$PATH`:
          ```sh
          chmod +x fleet-*-linux-* && sudo mv fleet-*-linux-* /usr/local/bin/fleet
          ```

          **Windows** — `fleet.exe` is installed to the app directory. Add it to `PATH`:
          ```powershell
          # In PowerShell (adjust path if installed per-machine)
          $dir = "$env:LOCALAPPDATA\Programs\Claude Fleet"
          [Environment]::SetEnvironmentVariable("PATH", "$env:PATH;$dir", "User")
          ```
          Or download the standalone binary and place it wherever you prefer.

          ## What's Changed

          See [CHANGELOG](https://github.com/${{ github.repository }}/commits/${{ github.ref_name }}) for full commit history.
          EOF

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Claude Fleet v${{ steps.version.outputs.version }}
          body_path: release_notes.md
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
          files: all-artifacts/*
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
